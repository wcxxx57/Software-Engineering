# 智映通学：本地运行手册

本项目使用 Docker Compose 一键启动前端、后端、数据库、消息队列、对象存储，以及四类 AI 内容生成服务。源码包不包含任何可用 Token；初始化脚本会自动创建服务内部使用的随机密钥，运行只需填写自己的 LLM API。TTS 音频生成api为可选配置，不填写时仍可生成无声视频。

## 1. 环境要求

- Windows 10/11、macOS 或 Linux；
- Docker Desktop，或 Docker Engine + Docker Compose v2；
- 建议至少 16 GB 内存、20 GB 可用磁盘空间；
- 一个兼容 OpenAI Chat Completions 的 LLM API；
- 可选的 TTS API；不提供 TTS 时，视频使用静音轨正常生成。

确认 Docker 可用：

```powershell
docker --version
docker compose version
```

## 2. 生成本地配置

Windows PowerShell：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\init-env.ps1
```

Linux 或 macOS：

```bash
bash scripts/init-env.sh
```

脚本会从 `.env.example` 生成被 Git 忽略的 `.env`，并随机生成数据库密码、JWT 密钥、服务回调密钥和对象存储密钥。脚本不会生成或写入任何第三方 API Token。

如果 `.env` 已存在，脚本默认拒绝覆盖。确需重建时，Windows 使用 `-Force`；Linux/macOS 使用 `FORCE=1 bash scripts/init-env.sh`。

## 3. 填写 API

用文本编辑器打开仓库根目录的 `.env`，只需填写以下三项：

```dotenv
LLM_BASE_URL=https://你的服务商地址/v1
LLM_API_KEY=你的LLM_API_KEY
LLM_MODEL=服务商支持的模型名称
```

说明：

- `LLM_BASE_URL` 必须指向兼容 OpenAI `POST /chat/completions` 的 API 根地址；
- `LLM_MODEL` 必须与服务商控制台中的模型标识完全一致；
- 默认 `K2V_TTS_PROVIDER=none`、`CODE_VIDEO_TTS_PROVIDER=none`。此时无需任何 TTS 凭据，知识视频和代码讲解视频仍会生成，但只能生成无声视频；

### 可选：配置语音合成

系统支持以下 TTS provider：

| Provider | 配置值 | 说明 |
|---|---|---|
| 静音 | `none` | 默认；不调用外部 TTS，生成带静音轨的视频 |
| OpenAI-compatible | `openai`、`dmx` | 调用 `POST /audio/speech`，供应商不限 |
| vivo 蓝心 | `vivo`、`bluelm`、`vivo_bluelm` | 调用 vivo WebSocket TTS |

使用 OpenAI-compatible TTS：

```dotenv
K2V_TTS_PROVIDER=openai
CODE_VIDEO_TTS_PROVIDER=openai

# 若 TTS 与 LLM 使用相同网关和 Key，下面两项可以留空。
TTS_BASE_URL=https://你的TTS服务商地址/v1
TTS_API_KEY=你的TTS_API_KEY
TTS_MODEL=服务商支持的语音模型
TTS_VOICE=服务商支持的音色
```

例如使用 OpenAI 官方接口时，可选择账户支持的模型和音色：

```dotenv
TTS_BASE_URL=https://api.openai.com/v1
TTS_MODEL=gpt-4o-mini-tts
TTS_VOICE=alloy
```

若使用 vivo 蓝心TTS api：

```dotenv
K2V_TTS_PROVIDER=vivo
CODE_VIDEO_TTS_PROVIDER=vivo
VIVO_TTS_APP_ID=你的VIVO_APP_ID
VIVO_TTS_APP_KEY=你的VIVO_APP_KEY
```

## 4. 校验并启动

在仓库根目录执行：

```powershell
docker compose --env-file .env -f compose.yaml -f compose.local.yaml config --quiet
docker compose --env-file .env -f compose.yaml -f compose.local.yaml up -d --build
docker compose --env-file .env -f compose.yaml -f compose.local.yaml ps
```

首次构建需要下载 Rust、Node.js、Python、LaTeX、Manim 和 FFmpeg 相关镜像与依赖，耗时取决于网络环境。命令执行完成后，`ps` 中的长期运行服务应为 `Up` 或 `running`；`minio-init` 正常完成后显示 `Exited (0)` 属于预期行为。

## 5. 打开系统并体验完整流程

浏览器访问：

- 主页面：<http://127.0.0.1:3080>
- 后端健康检查：<http://127.0.0.1:9000/health>
- RabbitMQ 管理页：<http://127.0.0.1:15672>
- MinIO 管理页：<http://127.0.0.1:9101>
- Knowledge2Video API 文档：<http://127.0.0.1:8080/docs>
- Code2Video API 文档：<http://127.0.0.1:8081/docs>

建议测试：

1. 在主页面注册并登录，可填写个人资料；
2. 新建一个计算机领域学习主题，例如“数据结构中的二叉树”；
3. 完成课前测试，等待系统生成个性化学习计划；
4. 进入学习任务，体验深度解析、知识导图与 2D 交互内容；
5. 生成知识视频或代码讲解视频；未配置 TTS 时视频正常生成但保持静音；
6. 完成课后小测，并在仪表盘查看学习进度、错题和资源记录；
7. 在仪表盘中单独体验知识讲解视频生成、代码讲解视频生成、2D交互等功能；
8. 查看金币、钻石、经验值的支出记录，签到和完成学习任务均可增加经验值。

AI 生成任务通过 RabbitMQ 异步执行。文本任务通常较快，视频任务需要执行脚本规划、TTS、Manim 渲染和 FFmpeg 合成，等待数十分钟属于正常情况。

## 6. 查看日志与排错

查看核心服务日志：

```powershell
docker compose --env-file .env -f compose.yaml -f compose.local.yaml logs -f --tail 200 backend frontend core-generation education2d knowledge-video-bridge knowledge-video-worker code-video-bridge code-video-worker
```

常见问题：

- `401` 或模型请求失败：检查 `LLM_BASE_URL`、`LLM_API_KEY`、`LLM_MODEL`；
- 视频没有声音：默认是静音模式；需要旁白时将两个 `*_TTS_PROVIDER` 改为 `openai`、`dmx` 或 `vivo` 并填写对应配置；
- 视频 TTS 失败：检查所选 provider 的 Base URL、Key、模型与音色；使用 vivo 时再检查 `APP_ID`、`APP_KEY` 和 `api-ai.vivo.com.cn`；
- 页面暂时打不开：执行 `docker compose ... ps`，确认 `frontend` 和 `backend` 已启动；
- 端口冲突：确认本机的 `3080`、`9000`、`15672`、`8080`、`8081`、`9100`、`9101` 未被占用；
- 修改 `.env` 后：再次执行 `docker compose ... up -d` 使配置生效。

## 7. 停止系统

停止容器并保留数据库与生成资源：

```powershell
docker compose --env-file .env -f compose.yaml -f compose.local.yaml down
```

不要使用 `down -v`，除非明确希望删除 PostgreSQL、RabbitMQ、Redis、MinIO 等本地数据卷。
